const bookLoader = require('./book.loader')
const authorLoader = require('./author.loader')
const bookshelfLoader = require('./bookshelf.loader')

module.exports = {
    bookLoader,
    authorLoader,
    bookshelfLoader
}