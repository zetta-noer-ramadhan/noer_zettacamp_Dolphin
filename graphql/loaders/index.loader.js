const songLoader = require('./song.loader')
const playlistLoader = require('./playlist.loader')
const userLoader = require('./user.loader')

module.exports = {
    songLoader,
    playlistLoader,
    userLoader
}